# SIMPLE solver example

This directory provides example input and mesh files for use with the SIMPLE solver application which solves the incompressible Navier-Stokes
equations. For the provided inputs to work, run the SIMPLE executable from the current directory, as follows:

``` $SFEM_INSTALL_DIR/bin/simple $INPUT.json``` 